namespace Qb.Domain.Enums
{
    public enum QuizStatus
    {
        Draft,
        Active,
        Completed,
        Archived
    }

    public enum Difficulty
    {
        Easy,
        Medium,
        Hard
    }

    public enum Roles
    {
        Standard,
        Admin
    }
}